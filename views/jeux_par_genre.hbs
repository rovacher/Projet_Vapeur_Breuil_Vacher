<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Jeux de {{genre.name}}</title>
</head>
<body>
  <h1>Jeux dans le genre : {{genre.name}}</h1>

  {{#if jeux.length}}
  <table border="1" cellspacing="0" cellpadding="5">
    <thead>
      <tr>
        <th>Titre</th>
        <th>Genre</th>
        <th>Éditeur</th>
        <th>Description</th>
        <th>Mise en Avant ?</th>
      </tr>
    </thead>
    <tbody>
      {{#each jeux}}
        <tr>
          <td><a href="/jeux/{{this.id}}">{{this.title}}</a></td> <!-- Lien vers les détails du jeu -->
          <td>{{this.genre.name}}</td>
          <td>{{this.publisher.name}}</td>
          <td>{{this.description}}</td>
          <td>{{#if this.mise_en_avant}}
            Oui
            {{else}}
            Non
            {{/if}}</td>
          <td>
          <a href="/jeux/{{this.id}}/edit">Modifier</a>
          <form action="/jeux/{{this.id}}/delete" method="POST" onsubmit="return confirmDelete()">
            <button type="submit" style="color: red;">Supprimer</button>
          </form>
        </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
  {{else}}
  <p>Aucun jeu trouvé pour ce genre.</p>
  {{/if}}

  <a href="/genres">Retour à la liste des genres</a>
</body>
</html>
